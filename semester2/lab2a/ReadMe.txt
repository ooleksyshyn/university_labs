Benchmark process:
1) create a queue : enqueue n elements to the queue
2) process with a queue : enqueue a new element and dequeue one (*n times)
3) delete a queue : dequeue n elements
benchmark.txt shows benchmark data for Vector, List and Array modes; algorithm complexity is O(n^2)
